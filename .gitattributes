# Ensure Windows-style line endings for batch scripts so cmd.exe parses them
# correctly regardless of the contributor's git autocrlf setting.
*.bat text eol=crlf
*.cmd text eol=crlf

# Leave other text files to git's defaults (per-platform native).
*.html text
*.css text
*.js text
*.json text
*.md text
